# SustainTech Lab - work in progress
# This site is not ready for public. No crawler, search engine or scraper
# is allowed to fetch any page. Remove these lines only when the site
# goes live (the noindex meta tag in _includes/head.html must be removed
# at the same time, otherwise the site will never be indexed).

User-agent: *
Disallow: /
